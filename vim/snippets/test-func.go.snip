func TestFoo (t *testing.T) {
  /*
	var guard *monkey.PatchGuard
	var guardB bool
	guard = monkey.Patch(){
		guard.Unpatch()
		defer guard.Restore()
		guardB = true
		return
	})
	setTestConfig()
	mc := new(MockImpl)
	mc.On("ImplMeth").Return()
	Foo(mc)
	assert.Equal(t, guardB, true)
  */
}
